<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidAssignment" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name = "colorPrimary"> @color/appBar</item>
        <item name="android:statusBarColor">@color/statusBar</item>
    </style>

    <style name="Theme.AndroidAssignment" parent="Base.Theme.AndroidAssignment" />
</resources>